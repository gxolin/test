import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'test-iap',
  webDir: 'dist/test-iap/browser'
};

export default config;
